export function DashboardRoute() {
  return (
    <section>
      <h1>Dashboard</h1>
      <p>
        Scaffold only. Add routes under src/routes/ and call the API through
        @prepmeup/api-client.
      </p>
    </section>
  );
}
